version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

# Configuración para construir la documentación
sphinx:
  configuration: docs/conf.py

# Instalación de dependencias para la doc
python:
  install:
    - method: pip
      path: .
    - requirements: docs/requirements.txt